import 'package:flutter/material.dart';

class AwarenessView extends StatelessWidget {
  const AwarenessView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Awareness"),
      ),
    );
  }
}
